{
  "name": "redux-persist-capacitor-storage",
  "version": "1.0.5",
  "description": "Redux persist storage adapter for capacitor filesystem storage across all devices",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc"
  },
  "author": "Halim Azeez",
  "repository": "https://github.com/Halimazeez/redux-persist-capacitor-storage.git",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@capacitor/core": "^1.3.0"
  },
  "keywords": [
    "redux",
    "redux-persist",
    "capacitor",
    "storage",
    "android",
    "ios",
    "electron",
    "filesystem"
  ]
}
